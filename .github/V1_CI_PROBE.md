# Temporary v1 CI probe

This branch-only file exists solely to trigger the pull-request QA workflow against the v1.0.0 release-candidate source. It must not be merged into `main` and will be removed by resetting the release-candidate branch after validation.
